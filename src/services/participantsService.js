let participants = [];

const getParticipants = () => participants;
const setParticipants = (p) => participants = p;

module.exports.getParticipants = getParticipants;
module.exports.setParticipants = setParticipants;
